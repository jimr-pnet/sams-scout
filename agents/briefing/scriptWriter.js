/**
 * Generates the briefing script from scored items using Claude.
 */
async function writeScript(items, options = {}) {
  throw new Error('Script writer not yet implemented');
}

module.exports = { writeScript };
